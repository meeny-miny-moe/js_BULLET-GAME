<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="madwhale.g82.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalendarActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".PhoneBookShowActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".PhoneBookShowDetailActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".PhoneBookInputActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".TodoShowActivity"
            android:theme="@android:style/Theme.Translucent"/>
        <activity android:name=".TodoInput"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".TodoShowDetails"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".TodoSearchActivity"
            android:theme="@style/Theme.AppCompat"/>


    </application>

</manifest>